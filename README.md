# python8
# 📄 File Handling & CSV Example in Python

This project demonstrates basic file operations in Python, including reading, writing, appending text files, handling exceptions, and working with CSV files using the `csv` module.

## 🚀 Features

- Create and write a text file  
- Read contents from a text file  
- Append data to an existing file  
- Exception handling for file operations  
- Create and write a CSV file  
- Read data from a CSV file  
- Ensure proper file closure using `with open()`

## 🧠 What You’ll Learn

This script teaches:

- How to perform basic file I/O in Python (`txt` and `csv`)
- Using `try/except` blocks for safe file handling
- Writing and reading structured data with the `csv` module
- Proper file closing practices to avoid resource leaks

## 📁 Files Included

| File Name                  | Description |
|---------------------------|-------------|
| `file_handling_example.py` | Main Python script demonstrating all tasks |
| `userdata.txt`            | Text file created by the script |
| `userdata.csv`            | CSV file created by the script |

> **Note:** The `.txt` and `.csv` files are generated when you run the script.

## 🧾 Requirements

- Python 3.x installed  
- No additional packages required (uses built-in `csv` module)

## 🛠 Usage

1. Download or copy the Python script file.
2. Open terminal/command prompt in the script’s directory.
3. Run the script:

```bash
python file_handling_example.py
